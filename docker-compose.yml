networks:
  1panel-network:
    external: true
services:
  mynotionnext:
    container_name: mynotionnext
    image: mynotionnext:latest
    ports:
      - "3100:3000"
    env_file:
	    - .env.local
    networks:
      - 1panel-network
    restart: always